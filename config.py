"""
Application-wide static configuration constants.
Keep this module side-effect free so it can be safely imported at module load time.
"""

# SQLite database filename used by the application. Change here to rename the DB file.
DATABASE_FILE = "vidnag.db"

# Settings directory name
SETTINGS_DIR = "settings"
